// get catetogy

const catPartent = document.getElementById("palleon-templates-menu");
console.l;
const array = [12, 3, 5, 43, 54];

let str = "";

// array.map((element, index) => {
//   console.log(element);
//   catPartent.innerHTML(
//     `<option value="banners-${element}">MU NAKEJKWE  - ${element}</option>`
//   );
// });
catPartent.append(
  <option value="banners-${element}">MU NAKEJKWE -{"element"}</option>
);

console.log(str);

// setTimeout(() => {
//   catPartent.innerHTML = str;
//   catPartent.insertAdjacentElement(catPartent, str);
// }, 1000);
